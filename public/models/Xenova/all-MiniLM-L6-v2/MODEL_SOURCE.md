# all-MiniLM-L6-v2 browser query encoder

- Source: `Xenova/all-MiniLM-L6-v2` on Hugging Face
- Upstream model: `sentence-transformers/all-MiniLM-L6-v2`
- Runtime artifact: `onnx/model_quantized.onnx`
- License: Apache-2.0

These files are served as immutable application assets. The semantic query Web
Worker loads them lazily and the service worker caches them after first use.

