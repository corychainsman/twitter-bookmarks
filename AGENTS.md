## Agent skills

### Ramp-up path

Keep this file as a lightweight routing layer. For architecture or coding work, read `CONTEXT.md` first. For procedural workflows, use the focused runbooks:

- Coding changes: `docs/runbooks/coding-workflow.md`
- Data refresh/export work: `docs/runbooks/data-refresh.md`

When inspecting code for variables, functions, types, or similar symbols, prefer `ast-grep` over plain text search.

### Issue tracker

Issues are tracked in GitHub Issues for `corychainsman/twitter-bookmarks`. See `docs/agents/issue-tracker.md`.

### Triage labels

Uses the default five triage labels. See `docs/agents/triage-labels.md`.

### Domain docs

Single-context repo: root `CONTEXT.md` and `docs/adr/` when present. See `docs/agents/domain.md`.
